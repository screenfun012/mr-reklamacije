/**
 * Zod schemas barrel export. Schemas are added per-module as API modules
 * are developed in later phases. Each schema lives in its own file:
 *   - emotive-claim.schema.ts (Phase 1)
 *   - domace-claim.schema.ts (Phase 1)
 *   - user.schema.ts (Phase 3)
 *   - etc.
 *
 * Do not add schemas directly here — keep the barrel thin.
 */
export {}
