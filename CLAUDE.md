# CLAUDE.md — MR Reklamacije (consolidated context)

> One file to read first. Distillate of `docs/*`, `.cursor/rules/*.mdc`, `CONTRIBUTING.md`,
> root config, and the working agreement with Nikola. When this file disagrees with what
> you "remember", **the repo wins** — and fix this file.
> **`.cursor/rules/*.mdc` are binding** (they block PRs); this is their summary, not a replacement.

---

## 1. What we're building

Internal **warranty-claims** system for **MR Engines** (Serbia) — remanufactured engines.
Two claim kinds: **EMOTIVE** (international partners) and **DOMACE** (domestic).
Flow: engine intake → claim → processing (observations, files, fault attribution by
employee/department/external party) → outcome (`pending` / `accepted` / `rejected` / `archived`).
Plus statistics, Excel import/export, OpenAI translation, and a client portal.

Domain terms kept verbatim (never translate/rename): **EMOTIVE, DOMACE, UKUPNO, MR NUMBER, GRESKA, APPROVED GREEN, GODINA**.

### Architecture — 3 isolated SPAs + 1 API

| Service        | Tech           | Role                                                                     | Domain / port                    |
| -------------- | -------------- | ------------------------------------------------------------------------ | -------------------------------- |
| `api`          | Hono + Node    | REST, auth, business logic, SSE — **the only thing that touches the DB** | no public domain / `:3000`       |
| `admin-web`    | TanStack Start | **Central control plane** (users, roles, šifarnici, audit, settings)     | admin.mrclaims.live / `:3001`    |
| `internal-web` | TanStack Start | Employees + viewers (claim processing)                                   | internal.mrclaims.live / `:3002` |
| `portal-web`   | TanStack Start | Clients (read-only, their own claims)                                    | mrclaims.live (apex) / `:3003`   |

- Frontends **proxy `/api/*`** to the API over Railway's private network → browser calls are same-origin; the api has no public domain at all. No browser CORS.
- **Physical isolation:** admin JS never ships to clients. **Host-only cookies per subdomain** — auth on internal ≠ auth on admin (intentional). Never import UI between apps; shared UI lives in `@mr/ui`.
- Stack: TanStack Start (React 19, Vite, SSR) · Hono · PostgreSQL + Drizzle · Better-Auth + custom RBAC · Paraglide (sr/en) · shadcn/ui + Tailwind v4 · ExcelJS · OpenAI · Vitest + Playwright · Railway + Cloudflare.

### Packages (`@mr/*`) and dependency direction (STRICT, CI-enforced)

`db` (Drizzle schema/migrations/seed/client) · `shared` (Zod schemas, enums, **permissions**, constants, pure utils) · `auth` (Better-Auth config + permission resolver) · `excel` · `i18n` (Paraglide) · `ui` (shadcn layer) · `logger` (pino). Tooling: `eslint`, `typescript`, `tailwind`, `vite` (`@mr/dev-vite`).

- `apps/*` → may depend on `packages/*`. **`packages/*` may NEVER depend on `apps/*`.**
- `packages/db` depends only on `packages/shared`. **No circular deps — ever** (depcruise enforces).
- Shared logic between two apps → goes in `packages/shared`.

### API module anatomy (mandatory per `apps/api/src/modules/<name>/`)

`schema.ts` (re-export from @mr/db) · `validators.ts` (Zod) · `repository.ts` (DB only) · `service.ts` (business logic, events, audit) · `controller.ts` (thin HTTP) · `routes.ts` (perm middleware) · `__tests__/`.
**Layer law:** controller never touches DB; service/repository never import `hono`/HTTP types. DI via `apps/api/src/core/container.ts` (constructor injection) — no module-level singletons/globals. `process.env` only inside `core/config/env.ts`.

---

## 2. Domain invariants — these are DESIGN, not bugs (don't "fix" them)

- **RBAC:** permissions are atomic, defined in code (`@mr/shared` permissions); roles live in DB; effective perms = union of role perms. **The server is the judge** — UI hiding & route loaders are courtesy; every route has `requirePermission('...')`.
- **Approving an account hands out SEVERAL privilege sets at once (2026-08-19):** `PATCH /api/users/:id/account-status` takes `roleCodes: string[]` (not one code) and assigns them in the approve transaction — rights add up, and a person normally needs two or three small packages. **The `client` set may never be combined with another one**: it is what makes an account see the portal and nothing else, and beside a staff set the same account would hold internal rights and a firm link at once. Refused in `UserAccountStatusPatchInputSchema`, explained in the approve dialog, and both halves are mutation-verified. The firm link is unchanged — choosing `client` still requires ≥1 EMOTIVE-partner customer, linked atomically.
- **Admin bypass:** `admin` role gets `ALL_PERMISSIONS` hard-coded in the resolver — never relies on `role_permissions` integrity. An admin can never be locked out.
- **NIKOLA-SAFE:** the protected super-admin (`screenfun99@gmail.com`) cannot have roles/status changed by anyone; self-role-change is forbidden. Enforced server-side.
- **Orphan engine-type protection** — intentional guard. Don't flag.
- **Claims rules (locked, `docs/04`):** EMOTIVE & DOMACE have **separate** detail routes/forms/loaders — never a shared `ClaimDetail` that branches on `kind`. `kind` comes only from the API, never inferred from field shape. One aggregate detail fetch per claim (JOIN in repo, route loader, `defaultPreload: 'intent'`). Create/update = one endpoint, one transaction (claim + faults). **No optimistic updates for claim create/edit** (only for small actions like status change, with rollback).
- **Claims are always editable (2026-07-18, editing-freedom Phase 1):** no outcome-based edit lock — a claim (either kind) is editable in any outcome state (`pending`/`accepted`/`rejected`/`archived`); every change is audit-tracked. Outcome change and delete both go through `<ConfirmDialog>` in the UI. The `emotive_claims.reopen`/`domace_claims.reopen` permissions and the `claim-lock.ts` guard layer were retired — don't reintroduce them. DOMACE repair-amount fields stay editable only when `outcome = accepted` (kept, unchanged). EMOTIVE client-facing outcome visibility was untouched by Phase 1 — it shipped as its own Phase 2, see the invariant below.
- **Svaka reklamacija ima kategoriju — red u šifarniku, nikad `enum` (Faza 1, 2026-08-20/21).** Obe tabele nose `category_id` → `claim_categories` (migracija `0045`, četiri reda: `REMONT_MOTORA`, `MASINSKA_OBRADA`, `NOVI_DELOVI`, `AUTO_SERVIS`; sve zatečene reklamacije backfilovane na `REMONT_MOTORA`). Kolona je **NULL-abilna u bazi, obavezna u Zod-u na KREIRANJU** — baza mora da primi ono što već postoji, a server je sudija za novi unos. **Na izmeni je opciona, namerno:** svaki editor na ekranu reklamacije šalje svoj deo (nalaz pregleda — a to je Gate A, kvarovi, nalazi), pa bi traženje kategorije na svakom PATCH-u sva tri pretvorilo u 400; a ništa ne bi ni garantovalo, jer polje nije `nullable` — izostanak ključa može značiti samo „ostavi kako jeste". Reklamacija ipak ne može da OSTANE bez kategorije: kreiranje je traži, a forma osnovnih podataka je uvek šalje. Šifarnik uređuje kancelarija iz admin panela (`settings.claim_categories.manage`); brisanje korišćene kategorije zaustavlja FK RESTRICT i vraća se kao 409. **Nijedan sloj ne sme da grana po vrednosti kategorije** — `if (category === 'MASINSKA_OBRADA')` je defekt koji obara pregled; filtriranje putuje KODOM (`?categoryCode=`), a jedina dva mesta gde aplikacija uopšte imenuje kod su `MACHINING_CLAIM_CATEGORY_CODE` i `ENGINE_OVERHAUL_CLAIM_CATEGORY_CODE` u `@mr/shared` (stavka menija „Mašinska obrada" i dva taba na portalu). **Ugašena kategorija OSTAJE na reklamaciji koja je nosi**: forma je drži izabranom, a server je prima na izmeni dok se ne menja — kreiranje u njoj i premeštanje u nju su 422 (`Invalid or inactive claim category`, proverava se kao i svaka druga referenca). Statistika je poštuje kroz JEDAN uslov u `buildActiveClaimWhere`, pa je poštuju sve sekcije, plus sekcija `byCategory`; spoj koji čita ime namerno NE filtrira obrisane redove — reklamacija nosi kategoriju, pa mora da je i imenuje. Klijentska žica nosi samo `categoryCode` (nikad id, nikad srpsko ime), a reklamacija u kategoriji za koju portal nema tab **ne dobija oznaku usluge** umesto pogrešne. Spec `docs/superpowers/specs/2026-08-17-claim-category-faza-1-design.md`.
- **EMOTIVE client visibility — private→published (Phase 2, 2026-07-18):** an EMOTIVE claim has two nullable `timestamptz` columns, `client_visible_at` and `published_at` (migration `0027`; existing rows backfilled `published_at = created_at` so nothing hides retroactively). Server-computed `clientPhase` (`deriveClientClaimPhase(outcome, { clientVisibleAt, publishedAt })` in `@mr/shared`) drives 3 client stages: **Primljeno** (both null) → **U obradi** (`client_visible_at` set + `published_at` null, or published-but-`pending`) → **Ishod** (`published_at` set + decided). **Gate A (auto, monotonic):** the first non-empty client-visible inspection report save sets `client_visible_at = COALESCE(client_visible_at, now())`. **Gate B (explicit):** `POST /api/emotive-claims/:id/publish` (permission `emotive_claims.publish` — operator + admin), idempotent, audits `{transition:'publish'}`, SSE `publishClaimUpdated`. **Masking:** while `published_at IS NULL` the client wire (`toClientClaimDetail`/`toClientClaimListItem`) masks `outcome`→`pending` and `dateOfFinish`→`null`; a Primljeno claim (both null) 404s on detail/attachments/client-PDF for `view_own_customer` (not 403 — don't leak existence). Visibility is **portal-only** — statistics, Excel, and internal views always read the real outcome; the outcome-change email only fires once a decided outcome is actually client-visible (`published_at !== null && outcome !== pending`). **Freshness — NEW/UPDATE (Phase 3, 2026-07-18):** a nullable `timestamptz` `client_content_updated_at` on `emotive_claims` (migration `0028_boring_sphinx`, existing rows NULL) bumps to `now()` on any **client-visible** content change (whitelisted field edit **to a meaningful value** — clearing/removal fires SSE only, not freshness; client-visible attachment **add** — remove fires SSE only, not freshness; Gate A, Gate B publish) — **internal-only edits (notes, faults, amounts) never bump it**, and it is **not monotonic**. Per-client-**user** last-seen lives in `emotive_claim_client_views(user_id, emotive_claim_id, viewed_at)` (composite PK, FKs cascade). The unified `/api/claims` list computes `freshness: 'new' | 'update' | null` per requesting client user (LEFT JOIN + CASE): shown when the claim is openable AND `client_content_updated_at` is set AND (never viewed OR changed since last view) — label **`new`** while `published_at IS NULL`, **`update`** once published. The claim detail GET (`GET /api/emotive-claims/:id`) is a pure read — it never records a view (decoupled 2026-07-18, see §9). "Seen" is recorded only via the explicit `POST /api/emotive-claims/:id/mark-seen` (204, same `view_own_customer` gate, Primljeno/non-owned → 404, no audit entry — this is view-tracking not a state change), which the portal fires once on real detail-view mount (not on hover-prefetch or the route loader) and which invalidates the client list + summary queries client-side so the badge clears without a reload; internal full-view reads never call it. Raw timestamps never leave the server — only the derived label. **EMOTIVE only** — DOMACE has no portal, its list branch always returns `NULL` freshness. **Per-section "Novo" markers (Phase 3.1, 2026-07-18):** a nullable jsonb `section_updated_at` on `emotive_claims` (migration `0029_mean_mantis`, map `{photos,inspection,details,outcome}` → last-change ISO ts, existing rows NULL) is bumped per-section at the same sites as `client_content_updated_at` — `inspection` on an inspection-report edit **to a meaningful value**, `details` on warranty/dates/engine/technician/mr edits **to a meaningful value** (clearing a field marks no key), `outcome` on publish, `photos` on a client-visible EMOTIVE attachment **add** (remove fires SSE only, not freshness); internal-only edits mark no key. The client detail (`GET /api/emotive-claims/:id` for `view_own_customer`) computes `sectionFreshness: {photos,inspection,details,outcome}` (booleans) in the same `findById` SQL, joined against the client's `viewed_at` as of read time (the GET is a pure read and never advances it — only `mark-seen` does, see §9 — so markers show for the whole visit and clear on the next open once mark-seen has fired); full-view/internal actors get all-false. Raw `section_updated_at` never leaves the server. Portal renders a "Novo" marker per fresh section on the detail cards plus a reduced-motion-aware pulse on the list freshness chip. EMOTIVE only; no new permission → no prod seed.
- **SSE = signal only:** events carry `type + kind + id`, never full payloads. Client calls `invalidateQueries`; never writes fetched rows into cache from SSE. Server is single source of truth. Transport is Postgres `LISTEN/NOTIFY` (`PostgresEventBus`, §9) — replica-safe, but the contract above is unchanged.
- **Admin = control plane (`docs/13`, binding):** every feature must leave an "admin hook" — (1) state changes write audit, (2) categories live in a registry not hardcoded, (3) gated actions map to a named permission, (4) catalogs are CRUD-able from admin, internal only reads.
- **Soft deletes only** for business data (`deleted_at`); repos filter `deleted_at IS NULL` by default. Forward-only migrations (revert = new migration).
- **Fault attribution:** a fault row has exactly one of `{employeeId, departmentId, externalPartyId}` (CHECK constraint), keyed by `fault_type`.
- **Prijem: zatečeno stanje je OBAVEZNO (2026-08-12).** Nalog se ne sme potpisati dok o stanju vozila nije zabeleženo bar nešto — **bar jedna stavka ček-liste sa DA ili NE, ili napomena o opremi**. Jedna funkcija je ceo propis (`isIntakeConditionRecorded` u `@mr/shared/utils`), a zovu je i čarobnjak (DALJE mrtvo na koraku Ček-lista) i servis (`sign()` baca 422) — nikad dve računice. **Prazan katalog prolazi bez ičega**: to je greška kancelarije i ne sme da zaustavi prijem auta u dvorištu (Nikolina odluka); papir tada nosi rečenicu `intake_print_condition_empty` kao dokaz, a ona od tog dana **nije normalan ishod nego mreža za tuđu grešku**. Odgovorena stavka koju je kancelarija u međuvremenu ugasila **i dalje vredi** — nalog je i dalje štampa sa njenim imenom. ⚠ Broj stavki se čita ISKLJUČIVO kroz `IntakeChecklistCatalogPort.countActiveItems()`; postojeći `listKnownCodes()` namerno zadržava ugašene kodove, pa bi njime potpuno ugašen katalog izgledao pun i zaključao pogon — mutacija je dokazala da tu razliku nijedan test nije hvatao, zato postoji test koji gasi ceo katalog pa potpisuje prazan nalog. **Napomena o opremi se od tada ŠTAMPA** (ispod kvačica u traci ZATEČENO STANJE, sečena kao primedbe): pravilo je priznaje, pa mora da stigne do papira — inače radnik zadovolji ekran a vlasnik dobije prazan dokument. Nema migracije, nema nove dozvole. Spec `docs/superpowers/specs/2026-08-12-intake-condition-required-design.md`.
- **Prijem: potpis zamrzava zapis (2026-08-11, deo H).** Potpisi prijema zatvaraju sve što je radnik uneo; posle njih se menjaju samo `services`, `materials` i `contactPhone` (`freeFieldsFor` u `@mr/shared/utils` je ceo spisak — i server i Spec kartica čitaju baš njega — a odbijanje ide na IME polja, ne na vrednost). Potpisan nalog se **ne briše**; fotografija se posle potpisa prima samo od nalogovog servisera i samo dok `photosPending > 0`, a ne briše se nikome. Razlog je vlasnikov odštampani papir: sve što se kod nas može pomeriti je neslaganje sa dokumentom koji je potpisao. **Režim izmene (V-6-2 — žig „menjano posle potpisa", dozvola `intake_orders.amend`, kolone `amended_at`/`amended_by`) je RETIRED — ne vraćati ga**; objava neslaganja je bila samo neslaganje. Drugo zamrzavanje, na primopredaji, sleće sa delom F.

- **Prijem: vozilo se vraća samo kroz primopredaju (2026-08-14, deo F).** `preuzeto` više nije prečka koju serviser gurne dugmetom. Poslednja prečka u zaglavlju naloga **vodi na ekran Primopredaja** (`/prijem/:id/primopredaja`): odozgo nadole šta je primljeno, šta je rađeno, izjava koja se potpisuje i dva potpisna polja. **PREDAJ VOZILO je mrtvo dok oba potpisa nisu tu, a rečenica ispod imenuje koji fali** — isti propis kao korak 1 čarobnjaka, i to je jedina dozvoljena vrsta mrtvog dugmeta. Vidljiv izlaz za vozilo koje je već otišlo: **„Predato bez potpisa"** (samo uz `intake_orders.change_status`, kroz `<ConfirmDialog>` koji imenuje posledicu). `status = 'preuzeto'` sa `handover_signed_at IS NULL` **jeste** zapis da je vozilo izdato bez potpisa — nema posebne kolone i ne sme je biti; u Istoriji ta stavka piše „Vozilo izdato bez potpisa", a potpisana „Vozilo predato uz potpise". Potpisi primopredaje su **drugo zamrzavanje** (`freeFieldsFor` u `@mr/shared` — posle njih ostaje samo `contactPhone`). Kartica DOKUMENT od tada nosi **oba** papira (`?kind=intake|handover` na `/document` i `/send-document`), a red primopredaje se pojavljuje samo ako je potpisana — izdato-bez-potpisa nikad ne dobija dokument. **`POST /:id/advance` staje jednu prečku ranije: `gotovo → preuzeto` odbija sa 409** čija poruka kaže gde ta promena živi — inače podela dozvola ne bi postojala, jer serviser drži `advance` pa bi vozilo izlazilo bez potpisa i bez nadređenog. Jedina druga vrata su kancelarijska traka za ISPRAVKU statusa (`POST /:id/change-status`) i ona se **ne zatvaraju**: traže `intake_orders.change_status`, istu dozvolu koju traži i „Predato bez potpisa" — ista vrata, isti ključ, i jedini način da se pogrešan tap vrati.

---

## 3. Working agreement with Nikola (HOW we work)

**Nikola is the owner, not a developer.** Explain clearly in **Serbian**; no unexplained jargon. He wants "a well-oiled machine — everything communicates smoothly, code clean, no patched holes (krpljene rupe)."

Cycle for every task: **PRE-CHECK (read/understand) → PLAN or show the proposal → STOP for approval → code → full gate → commit → STOP.**

- **You propose, Nikola/the operator approve before code.** No partial patches. No scope creep (find an unrelated bug → report it, don't fix it in the same change).
- **Migrations and auth are touched only with explicit approval.** For a migration: verify the journal first, generate via `drizzle-kit` (never hand-write SQL), prove clean migrate-from-zero on an empty DB, and confirm it's only the intended DDL before applying.
- **Show design decisions before applying** (e.g. color-token mappings, anything that changes visible behavior). If a needed token/resource is missing, propose the mapping and wait.
- **Full CI gate must be green before any commit.**
- **Commit only when explicitly asked**; use conventional-commit messages. An approved plan whose tasks each end in a commit **is** that ask — you don't re-ask per task.
- **You push, and the bar is the work, not permission** (changed by Nikola 2026-07-30; the old rule was "Nikola pushes, you don't"). Push when the work is genuinely finished: full gate green, no holes, no krpljenje, and nothing "packaged so it makes sense now and gives us a headache later" — his words. If any of that is missing, don't push and say why. Feature branches don't deploy (Railway watches `main`), so a push there costs nothing but protects the work; a push that puts unverified code in front of him is the thing this rule exists to prevent. After a push he restarts & verifies in the browser.
- **Never start or kill the dev servers.** `pnpm dev:all` lives in Nikola's terminal. For verification use one-off commands that exit (tests, `pnpm dev:check`), never touch his session.
- **Never interrupt `pnpm install`** (a partial install corrupts `node_modules`).
- When a rule/doc contradicts reality, say so explicitly and let Nikola decide — don't silently pick one.

---

## 4. Commands

```bash
# Full gate (what we run before every commit — all must exit 0)
# ⚠️ ALWAYS --force the cacheable tasks before a PUSH: turbo's local cache
# masked 3 CI failures on 2026-07-17 (stale api build/lint + shared test hits
# while their inputs had errors).
# ⚠️ **CI CACHES TOO** — the old line here said "CI runs cache-less", and the run
# that went red on 2026-08-17 reported `Cached: 13 cached, 26 total`. So a green CI
# does NOT mean every suite ran. `@mr/intake-document`'s timezone test landed on
# 08-13 and would have failed on any UTC machine from that day; CI only executed it
# four days later, when an unrelated `@mr/i18n` edit changed the package's cache key.
# The local `--force` run above is what actually covers everything — treat it as the
# gate, not CI.
# ⚠️ **Run the test pass once with `TZ=UTC`** — that is what CI and Railway both are, and
# this machine is Europe/Belgrade. On 2026-08-17 four date formatters read the machine's
# clock instead of the shop's; every one was correct here and wrong in production, where
# the API prints the owner's work order. Three of the four were found only after CI had
# already failed twice, because the first fix was verified in the wrong zone.
# ⚠️ Add `--concurrency=4` when `pnpm dev:all` is running (or on any busy machine).
# At the default (= core count) turbo runs three Vite builds AND every test suite at once;
# with the dev servers also on the CPU, timing-sensitive component tests starve. Measured
# 2026-07-26: packages/auth's two-factor suite takes 94 ms alone and 14.7 s under that load,
# blowing a 5 s testTimeout. The tests are fine — the machine was oversubscribed.
# ⚠️ **On this MacBook Air, run the tests in their OWN turbo pass.** `--concurrency=4` was not
# enough on 2026-08-18: two consecutive full gates went red on different packages
# (`internal-web#test`, then `portal-web#test`), and BOTH suites passed when run alone. Three Vite
# builds and the test suites at 4-wide starve the timing-sensitive component tests — the neighbours
# of the failure took 7.8 s for what normally costs under a second. A red like that is a lie about
# the code, and chasing it burns an hour. Split it:
pnpm format:check \
  && TZ=UTC pnpm exec turbo run build typecheck lint --force --concurrency=4 \
  && TZ=UTC pnpm exec turbo run test --force --concurrency=2 \
  && pnpm --filter api depcruise && pnpm test:integration
# (One pass is still fine on a bigger machine:)
# pnpm format:check && pnpm exec turbo run build typecheck lint test --force \
#   && pnpm --filter api depcruise && pnpm test:integration
# (CONTRIBUTING's pre-commit order: format:write, typecheck, test, test:integration, lint, depcruise, format:check)

# Dev (NIKOLA runs these — not you)
pnpm dev:all            # Postgres → API(:3000) → 3 frontends(:3001-3003), frees ports, waits for health
pnpm dev:check          # preflight: Postgres, API, ports, node_modules, phantom pins
pnpm dev:audit-deps     # after bumping better-auth / nitro / @tanstack/react-start

# Database
pnpm --filter @mr/db run db:migrate     # apply migrations (drizzle-kit, local dev)
pnpm --filter @mr/db run db:migrate:deploy  # prod migrator (Railway pre-deploy): required extensions + all migrations, idempotent, works from-zero
pnpm --filter @mr/db run db:seed        # idempotent SYSTEM seeds only (prod-safe: permissions, roles, departments, claim_sources, engine_manufacturers)
# ⚠️ Since 2026-08-18 the seed also DELETES: a permission gone from the catalog is pruned with the
# role grants that held it, and a system role's actions are synced to the code. It runs in ONE
# transaction, and it REFUSES (rolling everything back, printing each code + the sets holding it)
# when the prune would take an action away from a live role. Confirm with `-- --prune`. An orphan
# nobody holds still goes silently, so an ordinary seed needs no flag. Not run by any deploy —
# `preDeployCommand` is `db:migrate:deploy` only, so this is always a person typing it.
pnpm --filter @mr/db run db:seed -- --prune   # same, allowing the deletion of grants a live role holds
pnpm --filter @mr/db run db:seed:demo   # system + DEMO data (sample claims/employees/customers) — dev/test only, NEVER prod
pnpm --filter @mr/db run db:generate    # generate a migration from schema diff
pnpm create-admin                       # once per fresh DB

# DB access (dev)
docker exec -it mr-reklamacije-postgres psql -U mr -d mr_reklamacije
# host localhost:5433 · user mr · pass mr_dev_password · dev db mr_reklamacije · test db mr_reklamacije_test
```

### Restart procedure after touching `@mr/ui` or the Tailwind preset (stale CSS/dist lesson)

Dev resolves workspace packages from **`src/`** (the package `"development"` export + Vite `ssr.noExternal`), **not `dist/`** — but **typecheck/build read `dist` types**, and **preset/token (CSS) changes need Tailwind to regenerate**. So after editing `@mr/ui` or `tooling/tailwind`:

1. kill ports 3000–3003 (`lsof`/the dev runner) → 2. `pnpm --filter @mr/ui build` → 3. `pnpm dev:all` → 4. **hard refresh** (Cmd+Shift+R).
   Symptom of staleness: Vite logs `page reload packages/ui/dist/...`, or 504/login flicker (API starved by dist watchers). `pnpm dev:check` then `pnpm dev:all`.

### The API's dev runtime and JSX in a package (`tsconfig.dev.json`)

`apps/api` runs in dev as `tsx watch --conditions=development`, so workspace packages resolve to
their **source**. The moment one of them contains `.tsx` — `@mr/intake-document` does, it is the
printed work order — that source has to be transformed by tsx, and **tsx applies the `jsx` option
only to files its tsconfig INCLUDES**. `apps/api/tsconfig.json` includes `src` alone, so the
package's components were compiled with the classic runtime and every render died on
`ReferenceError: React is not defined` — in dev only, since production runs `node dist/server.js`
where the package supplies its own compiled JS.

Hence `apps/api/tsconfig.dev.json`: `jsx: react-jsx` with an include that reaches
`../../packages/*/src`, passed to tsx by the `dev` script. Nothing else uses it — `build`,
`typecheck` and the tests still use `tsconfig.json`, and none of them ever transform that source.

⚠ The failure is SILENT: sealing the document runs in the background behind a signature, so the
screen just goes on saying the document is being prepared. Found 2026-08-14 by walking a real intake
through the browser, which is the only place it could have been found.

### After touching `packages/i18n/src/messages/*.json`

**`pnpm --filter @mr/i18n run compile`, then refresh.** Paraglide generates
`packages/i18n/src/paraglide/` and dev reads _that_, not the JSON — so without the compile step
the screen calmly keeps showing the **old** text and it looks like the edit did not apply
(cost us a debugging detour on 2026-07-26).

⚠ **`compile` alone is NOT enough for the gate** (found 2026-08-14): `typecheck` reads
`dist/paraglide`, not `src/paraglide`, so a **new message key** typechecks red until the package is
**built**. Dev needs `compile`; the gate needs `build`. The old wording here said "the gate rebuilds
it", which is true of an existing key's text and false of a new key.

### Clean migrate-from-zero (proof before pushing a migration)

Empty DB needs these extensions first (the app's integration setup installs them; they are NOT in migrations):
`uuid-ossp`, `pgcrypto`, `citext`, `pg_trgm`. Then `drizzle-kit migrate` applies `0000..NNNN`. Integration tests' global setup already does migrate-from-zero on `mr_reklamacije_test`, so a green `test:integration` also validates the migration chain.

---

## 5. Brandbook & UI (`docs/09`, `docs/15`)

- **Colors only via `mr-*` tokens** in `tooling/tailwind/index.css`. Never hardcode Tailwind palette colors (`violet-*`, `amber-*`, `bg-[#...]`) — extend the preset instead. Dark mode via `dark:` + semantic tokens.
- **Semantic hues (now 6 + neutral):** `mr-brand` (red #ED1C24), `mr-error` (red #D92D20), `mr-warning` (amber), `mr-success` (green), `mr-info` (blue), **`mr-accent` (teal #0E9384 — 6th hue, brand extension, approved by Nikola)**, `mr-neutral` (grays). Each has `-strong` / `-subtle`.
- **All badges share `BADGE_SHELL_CLASSES`** (exported from `@mr/ui`) for consistent hover/transition, plus a color map in the `OUTCOME_BADGE_CLASSES` shape. Color constants for outcome/kind live in `@mr/shared/constants/*-colors.ts`.
- **Badge palette (role badge & status badge are always distinct in a row):**
  admin=`mr-brand` · operator=`mr-info` · viewer=`mr-neutral` · client=`mr-accent` || pending=`mr-warning` · approved=`mr-success` · rejected=`mr-error`. (admin-red vs rejected-red never co-occur — admin is the protected super-admin.)
- **Typography:** use `<Heading>` from `@mr/ui` (brandbook levels) — no ad-hoc `text-3xl`. Font **Figtree Variable** (+ JetBrains Mono for IDs/MR numbers). _(Note: `.cursor/rules/04-ui.mdc` still says "Inter" — outdated; brandbook + `docs/09` + perf rule say Figtree.)_
- shadcn/ui base, `lucide-react` icons only, `@tanstack/react-table`/`-form`/`-query`, `recharts`, `sonner`. Other libs need Nikola's OK.
- Destructive actions always go through `<ConfirmDialog>` (never `confirm()`/`alert()`). Skeletons not spinners. Empty/loading/error states on every list. Permission gating via route `beforeLoad` + a boolean prop the route computes from the session and passes down (UI is courtesy — the server is the judge). ⚠ **`<Can>` DOES exist** — `packages/auth/src/can.tsx`, exported from `@mr/auth/route-guards`, with its own tests in `route-guards.permissions-ui.test.tsx`. It is used by **no app**, and the line here claimed it did not exist at all between 2026-08-14 and 2026-08-18, when building the roles screen found the file. Keep gating the documented way (route + prop); reach for `<Can>` only deliberately, and if it stays unused, retiring it is its own decision — not something to do in passing.
- **internal-web imports Tiptap's `_variables.scss` with `layer(base)` — do not remove the `layer()`.** That file ends with `:root { &, *, ::before, ::after { transition: none … } }`, and **unlayered CSS beats every layer regardless of specificity**, so unlayered it put `transition-property: none` on every element and silently killed EVERY Tailwind transition in the app (`transition-colors`, hovers, the intake fuel needle) while the classes stayed in the markup and read as working. Found 2026-07-27 only because an animation "disappeared" after a reload. Symptom to recognise: a `transition-*` class computes `transition-property: none` with `duration 0.2s` and `cubic-bezier(0.46, 0.03, 0.52, 0.96)` — those are Tiptap's `--tt-*` defaults, not ours.
- **Animated SVG must transition the CSS `transform` property, never the `transform` attribute.** WebKit does not transition the attribute (measured 2026-07-27, Playwright: its computed transform stays `none` for the whole run) while Chromium does — so the attribute form animates on every desktop we test on and snaps on the iPad the serviser actually holds. Use `style={{ transform, transformBox: 'view-box', transformOrigin: '<x>px <y>px' }}`; it renders pixel-identically to `transform="rotate(a cx cy)"`. (`stroke-dashoffset` was measured the same day and DOES transition in both engines, attribute or property — so an arc that fills is safe.)
- **`var(--mri-<status-hue>)` does not resolve in internal-web — use the utility class.** `apps/internal-web/src/styles/globals.css` declares its theme-dependent tokens twice (`--mri-x` in `:root`/`:root:not(.dark)`, then `--color-mri-x: var(--mri-x)` inside `@theme inline`), but the status hues — `warn`, `ok`, `bad`, `info`, `domace` — exist **only** as `--color-mri-*` inside the inline theme, which Tailwind substitutes at build time and never emits to `:root`. So `stroke="var(--mri-warn)"` is an invalid `var()`, which drops the property to its initial value: `stroke`/`fill` become `none`/black and the shape silently disappears with no error in the console or the build. That is how the fuel dial's amber arc was invisible from the day it was drawn (found 2026-07-27). Use `stroke-mri-warn`/`fill-mri-ok`/`text-mri-bad`, or — if a runtime `var()` is genuinely needed — add the raw variable to both theme blocks the way `--mri-amb`/`--mri-grn` now are. Both instances in `intake-damage-map.tsx` were fixed in V-4c-0 (`5dee824`); the pattern is the thing to keep recognising, not the file.
- **admin-web has its own `--adm-*` block** (`apps/admin-web/src/styles/globals.css`), transferred from `design_handoff_admin_panel/admin-prototip.dc.html`: the 56px page grid (`.adm-grid`), the translucent top bar, the field fill, a primary button that is NOT the brand red, and six hues that carry a DIFFERENT value per theme (`grn/amb/blu/gry/teal/pur`). The shared preset is untouched — portal-web is a client-facing design of its own. Admin also keeps its own confirm dialog, pagination and control chrome (`~/lib/adm-chrome.ts`) because `@mr/ui`'s versions are shared with internal-web, whose screens nobody asked to move.
- **⚠ `bg-mr-surface-raised` is NOT a utility.** The preset routes `--mr-surface-raised` into shadcn's `--popover` but never names a colour after it, so the class compiles to nothing and the element comes out transparent — found on the admin confirm dialog, 2026-08-19, the same family of fault as internal-web's invisible amber arc. admin names it as `--color-adm-raised` (`bg-adm-raised`). Before reaching for `bg-mr-*`/`text-mr-*`, check the token is declared as a `--color-*` somewhere.
- **`.mri-input::placeholder` is `var(--mri-text2)` at 0.7**, transferred from the prototype. It used to be `currentColor` at 0.38, which inherits the control's near-black text in the light theme — the hints were rendered but unreadable, and read as missing.

---

## 6. Conventions distilled from `.cursor/rules/*.mdc`

**Style (01):** files `kebab-case`; components `PascalCase` (file matches); `camelCase` vars; `SCREAMING_SNAKE_CASE` constants. One primary export per file. Absolute imports (`@mr/*`, `@/`) — no `../../../`. `async/await` (not `.then`). Guard clauses over nesting. Functions < 30 lines. **No dead/commented-out code.** Comment **why**, not what. Throw typed domain errors (`NotFoundError`, `ForbiddenError`, `ValidationError`, `ConflictError`), never bare `Error`; never swallow errors. **No semicolons**, single quotes, 2-space, trailing commas; Prettier in CI. Conventional commits.

**TypeScript (02):** `strict` + `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitOverride`, `noFallthroughCasesInSwitch`, `noImplicitReturns`, `noPropertyAccessFromIndexSignature`. **`any` banned** in prod (tests/`.d.ts` only) — use `unknown` + narrow, or Zod. **No `enum`** → `as const` objects + derived type. `never` exhaustiveness in switches. **Non-null `!` banned** → assertion functions. Zod is the boundary source of truth (`z.infer`). Named exports (default only for React components / TanStack route files). No magic strings (constants in `@mr/shared`). Explicit return types on public/package exports, service/repo methods, and Query hooks.

**Testing (03, 10):** TDD expected. **Real Postgres in tests** — never mock DB/Zod/domain services/Hono. Always mock time (`vi.setSystemTime`), randomness, external APIs (OpenAI mocked except `[real-api]`). **Exception — SQL-driven time:** when the window under test is computed in Postgres (`CURRENT_DATE`/`now()`, e.g. the statistics rolling-24-month trend), `vi.setSystemTime` can't move it (it mocks only the JS clock) — so mocking would just desync JS-built fixture dates from the DB window. Instead **clamp fixture dates to stay inside the current period** (see `statistics.integration.test.ts` `daysAgo` — clamps to the 1st of the current month). Not a mistake; it's the root-cause fix for SQL-driven time. No `skip`/`only`/commented tests. Coverage (CI-enforced): services 90%, repos 85%, controllers 80%, utils 100%, overall API 85%, excel 95%, auth 95%, web components 70% (warn). E2E single-worker, stable selectors (`getByRole`), never `waitForTimeout`. Every bug fix ships a regression test. Test DB must end in `_test` (`assertIntegrationDatabase` refuses dev/non-`_test`).

**Security (05) — a security bug is SEV-1:** every route `requirePermission` (only `/api/health` exempt). Defense in depth (CF WAF → rate limit → auth → permission → service → repo → Zod) — never remove a layer. Validate every input with Zod (parse `:id` too). **Never leak** `password_hash`, secret `app_settings`, internal notes/attachments to client role. Row-level filtering for `view_own_customer`. Return **404 not 403** when a user lacks row-level access (don't leak existence). Drizzle parameterized only; `sql\`${param}\``ok, never`sql.raw`with user input. Secrets only in env /`app_settings(is_secret)`; gitleaks in CI. Files: magic-byte MIME check, size limits, paths from UUIDs, `Content-Disposition: attachment`+`nosniff`, signed URLs 5 min. Audit every state change (actor+IP+UA, action, entity, diff) — even admin. Session revoked on deactivate / password / 2FA change.

**API design (07):** `/api/<resource>/<id>/<sub>/<verb>`; paths kebab-case, query/JSON camelCase, DB snake_case. `GET`/`POST`/`PATCH`(preferred)/`PUT`(rare)/`DELETE`(soft). Status codes per table (201+`Location` on create, 204 on delete, 422 semantic, 409 conflict, 429 rate). List response `{ items, total, page, pageSize }`; single = bare object; error `{ error, message, code, details }`. Verb endpoints for non-CRUD (`/:id/change-outcome`, `/:id/approve`). Whitelist sort fields. Audit in the **service** layer (so direct calls audit too).

**Database (06):** schema in `packages/db/src/schema/*`, imported everywhere via `@mr/db`. Drizzle only; raw SQL via template literal when needed. Tables snake*case plural; FK `<singular>_id`; explicit index/constraint names (`idx*...`, `fk\_...`). Enum-like columns = `text`+ CHECK (extensible), not PG enum. Money`decimal(14,2)`. `date`for date-only (UTC midnight),`timestamptz` for moments. **Index FKs and WHERE/ORDER BY/JOIN columns** (Drizzle does NOT auto-create FK indexes) — but don't over-index. Multi-row writes in a transaction. Seeds idempotent (`onConflictDoNothing`), split: `runSystemSeeds`(permissions → roles → departments → claim_sources → engine_manufacturers; prod-safe) +`runDemoSeeds`(employees → customers → engine_types → demo claims; dev/test only — integration globalSetup runs both; real data comes from`import-legacy`). **NEVER** `TRUNCATE`/`DROP`prod, connect to prod from local (except read-only tunnel), ship data-deleting migrations without Nikola's OK, or`ON DELETE CASCADE` on business tables except clear parent-child (faults).

**i18n (09-rule):** every user string via Paraglide `m.*`; keys English `namespace_context_variant` (`action_save`, `validation_required`, `nav_emotive_claims`). Both `sr.json` + `en.json` required (CI checks parity). Serbian is primary, informal "ti" form, follow the glossary (always "Prilog", never a synonym). **NO ICU plurals** — `{count, plural, …}` crashes this repo's Paraglide compile (verified 2026-07-26; the real error hides under a huge base64 blob in the output, and there is not one plural message in the codebase). Phrase counts so no grammatical form depends on the number: "Ukupno: 12", never "{count} naloga", which reads "1 naloga". Never concatenate translated strings — interpolate. Don't translate IDs/MR numbers/proper nouns; format dates/numbers via `Intl.*`.

**Performance (10):** queryOptions factory in `@mr/shared/src/queries`; route loaders prefetch (`ensureQueryData`); `useSuspenseQuery` + Suspense over `isLoading`. Selective columns in list queries, full row + relations in detail. JOINs never N+1. Index FK + WHERE columns. Debounce inputs 300ms. Lazy-load heavy routes. Bundle budgets: admin/internal < 250KB, portal < 150KB gzipped. API p95 < 200ms, DB query p95 < 50ms. _(See drift note on pagination & optimistic updates below.)_

**Anti-patterns (08) — never commit:** god files (>500 lines), magic numbers, copy-paste ×3 (extract), N+1, swallowed errors, mixed concerns in controllers, TODO without issue/date, module-level mutable state / global singletons, `any` to silence, nested ternaries (use a lookup map), commented-out code, `utils.ts` dumping grounds, fetching in `useEffect` (use Query), `useEffect` for derived state, direct DOM/`dangerouslySetInnerHTML`, `console.log` in prod (use pino), boolean param traps (use options object), re-inventing libs, hardcoded URLs (use relative `/api/...`).

**Phantom deps (CONTRIBUTING):** these are pinned because upstream under-declares them — **don't remove casually**: `@opentelemetry/api@1.9.1`, `jose@6.2.3`, `kysely@^0.28.14` (apps/api, via better-auth), `youch@4.1.1` + `youch-core@0.3.3` (root, via Nitro). Re-audit with `pnpm dev:audit-deps` after bumping better-auth / Nitro / @tanstack/react-start.

---

## 7. Where things live

- **Schema / migrations:** `packages/db/src/schema/*.ts`, `packages/db/migrations/` (+ `meta/_journal.json`). Seeds: `packages/db/src/seed/` (`run-system-seeds.ts` orchestrates). Test helpers: `packages/db/src/test-helpers/integration-*.ts`.
- **Permissions / roles / enums / Zod / query factories:** `packages/shared/src/` (`permissions.ts`, `constants/`, `schemas/`, `queries/`).
- **Auth:** `packages/auth/src/` (`better-auth.config.ts`, permission resolver, `revoke-user-sessions.ts`).
- **API core:** `apps/api/src/core/` (`container.ts`, `middleware/` incl. `require-permission.ts`, `errors/`, `config/env.ts`). Modules under `apps/api/src/modules/<name>/`. SSE in `modules/events/`. Excel in `modules/excel/` + `@mr/excel`.
- **UI tokens/badges:** `tooling/tailwind/index.css` (tokens), `packages/ui/src/lib/badge-styles.ts` (`BADGE_SHELL_CLASSES`), `packages/ui/src/components/*-badge.tsx`. Admin user badges: `apps/admin-web/src/components/users/`.
- **Brandbook:** `docs/15-brand-guidelines.md` (authoritative) + `MR Engines Brandbook.pdf`.

---

## 8a. Known issues (real bugs, fix later — don't trip over)

- ~~Intake wizard, step 1: NEXT is dead and the sentence does not say why~~ **FIXED 2026-08-14**: the
  footer recited a fixed list of four fields while the button waited on six. `step1Missing()`
  (`intake-wizard-state.ts`) is now the whole rule and `step1Complete()` is derived from it, so the
  sentence and the button cannot drift again; `step1MissingLabels()` names the empty fields with the
  labels printed on their own rows ("Fali: Broj lične karte"). Regression tests in
  `intake-step1-hint.test.ts`; both mutations (the ID rule, the label map) verified red.
- ~~Claim-report PDF prints Serbian letters in the wrong typeface~~ **FIXED 2026-08-14**: the
  hand-written `@font-face` carried no `unicode-range`, so it claimed the whole alphabet while
  embedding the `latin` subset alone and Chromium drew `č ć ž š đ` from the next family in the list.
  Both PDF paths now share `core/pdf/inline-font-stylesheet.ts`, which takes fontsource's stylesheet
  whole so every subset's range comes from upstream. ⚠ **The family name comes with it**: fontsource
  declares `Figtree Variable`, so `CLAIM_REPORT_EXPORT_FONT_FAMILY` had to change too — swapping the
  embedding alone would have left nothing matching and sent the WHOLE report to the fallback. Proven
  by measurement, not reasoning: before the fix `ččč` measured exactly the fallback's width while
  `ccc` measured Figtree's; after, both are Figtree's.

- ~~Users-list keyset pagination broken past page 1~~ **FIXED**: `users.repository.ts` now mirrors the audit-log pattern (`created_at::text` cursor compared via `::timestamptz`); regression test in `users.integration.test.ts` ("paginates past the first page"). All other keyset usages key on text/integer columns and were already fine.
- ~~Portal claims list client-side pagination over ≤50 (latent cap)~~ **FIXED** in the portal v2 redesign: `clientClaimsListOptions(page)` is server-side paginated (10/page), no cap, no client-side slice; `portal_claims_capped` caption removed.
- ~~`/api/dashboard/summary` leaked GLOBAL data to portal clients~~ **FIXED** (SEV-1): the gate accepted `view_own_customer` and the queries had no customer scoping, so a client could read other customers' names/MR numbers. Now `/summary` requires full `emotive_claims.view`/`domace_claims.view` (route + service, defense in depth); clients use the scoped `/api/dashboard/client-summary` projection. Regression tests in `dashboard.integration.test.ts`.

## 8. Known drift (docs/rules vs reality — fix later, don't trip over)

- **UUID v7 vs v4:** `.cursor/rules/06` + `docs/02` say "UUID v7 only, `crypto.randomUUID()` forbidden for PKs." **Reality:** schema uses `uuid('id').primaryKey().defaultRandom()` (v4). Repo reality wins for now. **Decision for later (not in passing):** either migrate the schema to v7 (sortable IDs → better index locality) or relax the rule to "v4 OK". Pick deliberately; don't change PK generation mid-task.
- **Integration test isolation:** `docs/10` + `DEV_SETUP` say "transaction-per-test (BEGIN/ROLLBACK)." **Reality:** shared `mr_reklamacije_test` DB; global setup migrates+seeds once; suites use `TRUNCATE`/manual delete and must seed their own prerequisites. **This drift is not theoretical — it already caused an order-dependent failure** (a new suite called `seedRoles` without seeding `permissions` first; another suite's `TRUNCATE` wiped them → FK 23503). Fixed in **commit `18241cd`** by seeding permissions before roles. Lesson: when writing a new integration suite, **seed permissions before roles** (or call `runSystemSeeds`); never rely on another suite's seed surviving.
- **Pagination:** `rules/06` says offset OK for MVP; `rules/10` says cursor for >100 rows. Current claims lists use offset. Treat cursor as the target for large lists, not a retrofit mandate.
- **Optimistic updates:** `rules/10` C4 says "every mutation optimistic"; `docs/04` (claims, locked) says **no optimistic for claim create/edit**, only small actions; `docs/05` SSE is invalidate-only. The **claims rule wins for claims**; optimistic is fine for small toggles with rollback.
- **Font:** `rules/04-ui` says Inter — outdated; use **Figtree**.
- **`users.account_status`:** reality is ahead of docs — migration `0016` added `account_status` (`pending`/`approved`/`rejected`) and the approval flow assigns a role on approval (`operator`/`viewer`/`client`, super-admin excluded). `docs/05`/`docs/02` still describe only `is_active` + `client_registration_requests`; the user-approval path is the newer mechanism. **Approving as `client` also links the user to ≥1 EMOTIVE-partner customer via `customer_users` (atomic, in the approve transaction; gated by `customers.link_users`)** — that link is what the `own_customer` row-level scope reads.
- **`client_registration_requests` is a DEAD table — do not use it.** It exists in the schema (`packages/db/src/schema/client-registrations.ts`) + has an enum (`ClientRegistrationStatus`), but has **zero** endpoint/service/UI wiring. The live registration→approval mechanism is the unified `users.account_status` flow above. Don't mistake the table for the path; if client self-registration is built (portal Phase C), decide deliberately whether to revive it or stay on `accountStatus` (current direction: stay on `accountStatus`).
- **Connection pool size:** RESOLVED — reality is node-postgres defaults (max **10**, idle 10 s, `connectionTimeoutMillis: 0`); `rules/10`'s "20" is wrong. Explicit pool options (+ statement_timeout) remain a deliberate future choice.

---

## 9. Current state / recent work (this collaboration)

- **Faza 1 — kategorija reklamacije (2026-08-20/21, grana `feat/claim-category`, 15 komita `fcf4e50`..HEAD, pun gejt zelen, pushovano, NIJE na `main`-u):** model iz §2 sproveden kroz ceo stek — migracija `0045` (šifarnik + dve kolone + backfill), API modul `claim-categories` po šablonu kataloga, admin ekran, polje u obe forme i na oba detalja, kolona i filter u listi, statistika (jedan uslov + sekcija „Po kategoriji"), stavka menija „Mašinska obrada" koja je zamenila ekran-obećanje, i portal koji više ne tvrdi da mašinskih reklamacija nema. **Portalski filter ide na server** (`service` u URL-u → `categoryCode`) jer je lista strankirana pa bi filtriranje posle dolaska podataka razminulo brojač strana sa spiskom. **Rupa nađena u završnom pregledu i zatvorena:** kategorija je bila jedina referenca koju servis nije proveravao pre upisa — nepostojeći id se vraćao kao neuhvaćena FK greška (500), a ugašena kategorija je prolazila u tišini; sada je provera ista kao za tip motora i proizvođača, na obe familije. **Posle merge-a u `main` treba JEDNOM** `pnpm --filter @mr/db run db:seed` u api Console (nova dozvola `settings.claim_categories.manage`); migracija ide sama kroz `db:migrate:deploy`, a četiri kategorije su u njoj pa katalog ne traži seed.

- **Admin panel wears the prototype (2026-08-19/20, branch `feat/permission-holes`, six commits `25e2015`..`8b62909`, full gate green — MERGED to `main` since, tip `588d3e9`):** Nikola asked for a written description of what the panel DOES, gave it to "Claude Design", and brought back a working clickable prototype. **The visual source of truth is `design_handoff_admin_panel/admin-prototip.dc.html`** (committed with its `support.js`; the designer's own handoff is `docs/design-handoffs/2026-08-19-admin-panel-prototype-handoff.md`, the functional description `docs/superpowers/specs/2026-08-19-admin-panel-design-handoff.md`). ⚠ **Values are READ from that file, never estimated** — serve the folder over HTTP and drive it in a browser. What changed: tokens + frame (grouped sidebar with an amber count of waiting accounts, ADMIN chip, mono breadcrumb) · dashboard now draws `recent`/`overdue`/`chart`, which the server had been sending and the screen threw away · ten catalogue screens through one file · the 84-action matrix in three columns · users (see the approve invariant in §2) · audit · settings, security, sign-in. **The only server changes are the approve payload (§2) and `GET /api/dashboard/summary?months=`** — clamped 6…24, default unchanged, part of the summary cache key, and only admin asks for 24 (internal-web keeps six). Both dashboard invalidation paths now name the `dashboard` prefix rather than one window. Deliberate deviations from the prototype, each Nikola's call: the real logo stays (the prototype types the wordmark), there is no "+ Novo ovlašćenje" (copying a set is the only way in), and **"Vrste oštećenja" / "Način dolaska" stay in the menu as honest "does not exist yet" screens** — the lists behind them are still `IntakeDamageType`/`IntakeArrivalMode` constants in intake code. **Mobile (390) is deliberately not started** — agreed to follow desktop approval.
- **Every email arrives in one frame (2026-08-14):** the four messages this system sends — intake
  document, activation, EMOTIVE outcome change, new client submission — all render through
  `apps/api/src/core/email/email-layout.ts`. **A fifth email must use it too; do not hand-roll one.**
  It is in `core/` because four modules consume it and a module may not import a module. The frame is
  the intake document at inbox width: black `#17171a` band with the emblem, white 600px sheet, red
  `#ed1c24` rule above a footer carrying `PORTAL_SUPPORT_PHONE` (+ a contact email when the recipient
  has an inbox that answers — portal-facing messages only). Four constraints are load-bearing and were
  each chosen against an alternative: **tables and inline styles only** (Outlook renders mail with
  Word's engine — no flex, no grid, and `<style>` is dropped on some Gmail accounts), **no Figtree**
  (Outlook blocks web fonts, Gmail ignores `@font-face` — the system stack is what actually arrives),
  **the emblem is a remote image** from `https://mrclaims.live/portal/logo-white.png` with white bold
  `alt="MR ENGINES"` so the band still reads when a client blocks images, and **`color-scheme: light`**
  or Apple Mail and Outlook invert the palette themselves and the band turns to mud. `escapeHtml` lives
  here too — it had been copy-pasted into all four. The red button is Nikola's call (2026-08-14) over
  the band's black: the brandbook keeps red off primary buttons _inside_ the apps, an email is not one.
  `client-submissions` was the one message with hardcoded Serbian outside Paraglide; it now uses
  `email_submission_*` keys, rendered at a fixed `sr` because it goes to ONE shared office address with
  no recipient whose language could be looked up. `PORTAL_SUPPORT_PHONE` = `062/1144888` since
  2026-08-14 (it feeds the portal support card as well as every email footer — one constant, two
  places). No migration, no permission, no wire change.

- **DOMACE claims → full business Excel (2026-07-24, docs/23, branch `feat/domace-full-fields`):** brought the DOMACE model + export up to the real 15-column `Domaće reklamacije.xlsx`. **Migration `0035`** adds four nullable `domace_claims` columns: `invoice_number` (BROJ RAČUNA H), `original_invoice_amount` (G), `parts_amount` (K), `labor_amount` (L). **Money model:** UKUPNO (M) stays `total_amount` but is **computed = parts + labor, ex-VAT** (shared `computeDomaceTotal`, written on every create/update so statistics/dashboard read one column). Amounts are **editable in any outcome state** — the accepted-only guard (`core/claims/claim-lock.ts`) and the `PATCH /:id/amount` endpoint are **retired**; amounts flow through the normal create/update. **Field identities:** RADNI NALOG (E) = `mr_number`; **STARI R/N (F) = the existing `claim_number`** (relabelled "Stari radni nalog"); BROJ RAČUNA (H) = the new `invoice_number`; OPIS PROBLEMA (I) = `warranty_report`; VOZILO (D) = **composed** from manufacturer + engine type + engine code (no new column). **In-app labels renamed to match the sheet** (DOMACE only; DATUM kept "Datum prijema" by Nikola). **ZAPOSLENI (N)** now lists **every active employee** (not assembly-only) via a **searchable** select — DOMACE-specific override of the assembly-only assigned-worker rule. ⚠️ **NAPOMENA (O) = the claim's findings, composed `text (type); …` — this REVERSES the earlier invariant** "Excel must NOT export findings / findings→NAPOMENA rejected" (was CLAUDE.md §9 / docs internal-web-backlog #4). Reversed deliberately with Nikola: DOMACE has no portal, so the client-leak reason was moot. The findings SECTION is titled "Napomena" in the DOMACE detail (EMOTIVE keeps "Nalazi"). Excel export helpers `formatVehicle` + `formatFindingsNote` live in `@mr/excel`; the DOMACE sheet went 9→15 columns. **Portal: zero impact** (DOMACE has no customer FK); **EMOTIVE untouched**. Built phase by phase (M-0 migration → M-1 api/shared → M-2 form+labels → M-3 excel), each gate-green.

- **PITR enabled + attachment-integrity check (2026-07-23):** point-in-time recovery is now **on** for the production Postgres (Railway → Postgres → Backups). Restore creates a **new** Postgres service and leaves the running one untouched — so, unlike a volume "Restore" (which overwrites in place and deletes newer snapshots), it is the only safe way to rehearse a database restore. Reversible via "Stage disable". The one-time redeploy was verified non-destructive (`/api/claims` → 200, 134 rows, right after). New read-only script `apps/api/scripts/check-attachment-integrity.ts` (`pnpm --filter api check-attachment-integrity`, no flags) walks every `attachments` row and asserts its object exists in storage via the SAME backend the runtime picks (`createStorageService`), exiting 1 when any file is missing — so it doubles as a **post-restore gate**. This answers the real fear behind the DB/bucket split (docs/22 §1.3): a row pointing at an object the restored bucket never held is silent (the claim opens, the photo is blank). The full restore _drill_ still needs a throwaway environment for the bucket half (deferred with staging); PITR unblocks the database half. ⚠️ Watch for `*/` inside a block comment — `S3_*/UPLOAD_DIR` closed the JSDoc early and broke the esbuild transform.

- **Request ids + export timeouts (2026-07-23):** `requestId()` (`hono/request-id`) runs right after the secure headers, so **every log line for one request shares an id** — the request-logger line and every branch of `app.onError` — and the middleware echoes it as `X-Request-Id`, so an id read from the browser's network tab is the one to grep for. This is the "a performance complaint that cannot be reproduced" item from docs/22 §3, answered without an APM vendor. `AppVariables` now extends `RequestIdVariables`. ⚠️ **Latent bug found while wiring this: `app.onError` had no `HTTPException` branch** — anything Hono's own middleware raised fell through to the catch-all, so it reached the client as a 500 and was logged as "unhandled server error". Fixed with a branch that maps `HTTPException` into the standard envelope (`HTTP_EXCEPTION_CODES`, 504 → `SERVICE_UNAVAILABLE`); this was a prerequisite for the timeout below to report as a timeout at all. `exportTimeout` (`core/middleware/export-timeout.ts`, 60 s, shared by claim-report and Excel export routes) bounds the two requests that can legitimately run for seconds: a Chromium render waits behind a 2-slot queue and an ExcelJS workbook is built entirely in heap, so a stuck one previously held its slot until Cloudflare cut the connection at ~100 s and showed its own page. Deliberately generous — a backstop, not a performance target.
- **Restore ORDER is now written down (`docs/11`, 2026-07-23):** the database and the attachment bucket back up on **independent schedules**, so restoring them to different moments yields claim rows pointing at objects the restored bucket never held — silent at restore time, surfacing weeks later as "photos are missing from old claims". PITR makes this _easier_ to get wrong, since the fine control is on the database half only. Order: restore the bucket first → read its snapshot timestamp → bring the database to THAT moment. Round _backwards_ if you must. Verifying a restore means opening three claims with photos and seeing the images — a restored database with an empty bucket starts perfectly.

- **Node 20 → 24 (2026-07-23):** Node 20 left support on **2026-04-30**, so the runtime had been ~3 months without security patches (the AWS SDK said so in the prod logs). Shipped in two steps — 20 → 22, then 22 → 24 once the blocker below was understood. Node 24 is the **Active LTS** (EOL April 2028); 22 is already Maintenance (April 2027), and odd releases never become LTS. Node 26 exists but is still **Current** until October 2026 and removes legacy APIs — not a production target yet. Bumped `.nvmrc`, `apps/api/Dockerfile` (`node:24-bookworm-slim`), root `engines.node`, **`.github/workflows/ci.yml` `node-version`** — CI must run what production runs, or a green gate proves nothing — and aligned every `@types/node` to `^24.13.3` (they had drifted: `packages/excel` was on 22, the rest on 20).
  ⚠️ **The blocker was a TEST, not the runtime — and the first diagnosis was wrong.** `apps/portal-web/.../claim-detail-mark-seen.test.tsx` loaded the route with `await import('~/routes/claims/$id')` **inside the test body**; that route pulls in the whole detail screen, and transforming it costs seconds charged to the FIRST test's `testTimeout: 15000`. It looked Node-24-specific because a fast laptop passed on 20/22 and failed on 24 — then CI (two cores) failed on **22** with the identical pair of failures. The second failure ("Found multiple elements with the text: MR-7167") is only a cascade: that render lands after `cleanup()`. Fix = a **static** import, so the cost moves to collection, which `testTimeout` does not bound; measured on Node 24, test time **1384 ms → 93 ms** and collect 1.09 s → 2.70 s. `cleanup()` IS registered in `vitest.setup.ts` (that hypothesis was tested and disproved), and the timeout was deliberately NOT raised. **Lesson: never load a heavy module inside a test body.**
  ⚠️ **`.nvmrc` was in no service's `watchPatterns`**, so a runtime bump alone could never trigger a Railway deploy. Combined with a Wait-for-CI skip, that left portal-web on the new Node while api/internal/admin stayed on the old image. `.nvmrc` is now watched by all four `railway.json`. Wait-for-CI is confirmed working (the red commit sat SKIPPED). Verified in prod after deploy: all four services successful, no 5xx, and `/api/claim-reports/export/pdf` returned **200** — Chromium renders on the new runtime. The production image could not be built locally (Docker Hub pull times out here); Railway built it.

- **Load-stability pass (2026-07-23, branch `perf/load-stability`, gate green, NOT yet on main):** three fixes from a health/perf audit whose findings are measured, not guessed (prod `pg_stat_statements`, Railway metrics, an `EXPLAIN` on prod, and a live request count).
  **(1) Rate limiting is now TWO layers, both of which must pass.** `generalRateLimiter` stays keyed by IP but its ceiling went **100 → 600/min**: it is a volumetric flood backstop, NOT a per-person quota — the whole office shares one public address behind NAT, and SSR calls carry no client address at all, so that bucket legitimately holds everyone's traffic. New `sessionRateLimiter` (120/min) is keyed by a **sha256 of the session cookie** (never the raw token) and is **skipped entirely** when no session cookie is present. `RateLimitOptions.keyOf` may now return `null` = skip this layer. ⚠️ **Session keying may only ever ADD a limit, never replace the IP one:** cookies are client-chosen, so a "key by cookie, else by IP" fallback would let an anonymous caller mint a fresh bucket per request by rotating a made-up token. A regression test pins exactly that. The pre-session flows (sign-in, sign-up, client registration, activation) inherit the IP-keyed default deliberately.
  **(2) SSR calls now forward the real client address.** `applyForwardedRequestHeaders` (`packages/shared/src/api/forward-request-headers.ts`) is the single policy for which headers cross: `cookie` + `cf-connecting-ip`, never overriding a header the caller set. Wired into all THREE outbound SSR paths — `fetch-json.ts`, `fetch-no-content.ts` and `packages/auth/src/server-session-loader.ts`. Before this, every server-rendered page load of every user landed in one shared `'unknown'` bucket and audit rows recorded `actorIp = null` (prod logs showed Better-Auth's own `Rate limiting skipped: could not determine client IP address`). `x-forwarded-for` is deliberately NOT forwarded — from inside the private network its rightmost entry is our own container. ⚠️ **The ambient-request import (`await import('@tanstack/react-start/server')`) must stay INSIDE each package that needs it** — resolved across a package boundary it leaves the consumer's module graph, so a consumer mocking that module silently gets the `catch` branch and forwarding degrades to nothing in a test that still passes. Only the pure policy is shared. The `getRequestHeaders` calls in `@mr/i18n` read the locale cookie and make no API call — untouched on purpose.
  **(3) The shared Chromium releases itself after 10 min idle** (`IDLE_SHUTDOWN_MS` in `claim-report-export-pdf.ts`). Measured on Railway: the api sat at 150–180 MB and held **650–820 MB for a full hour** after a single PDF export, reset only by the next deploy — and memory is **93 % of the hosting bill**. An `activeRenders` counter keeps a render queued behind another one from losing its browser; the timer is `unref()`ed so it never holds the process open; `dispose()` clears it. The next export relaunches (~1–3 s cold start).
  No migration, no new permission, no frontend change. Measured context worth keeping: prod DB is 12.6 MB / 2-of-100 connections / 99.99 % cache hit, the auth path's 3 queries per request cost **875 ms across 16 days**, and `idx_emotive_claims_search_fts` is confirmed usable (`Bitmap Index Scan` under `enable_seqscan=off`) — its 0 scans are table size, not expression drift. Full audit + phased plan in the session scratchpad `PLAN-RADA.md`.

- **Assigned-worker department filter + notification delete (2026-07-23):** two features.
  **(1) Assigned worker = assembly-only, admin-configurable.** New boolean `departments.provides_assigned_workers` (migration `0034_bouncy_ben_parker` — one column default false + backfill `SKLAPANJE = true`; the demo seed also flags SKLAPANJE for fresh DBs). The claim **"zaduženi radnik"** field now lists only workers whose department has the flag; **fault attribution deliberately still lists EVERY worker** — the two must use different queries. `@mr/shared`: `EmployeesListQuery.assignableOnly` (a required boolean after the zod transform, like `activeOnly`), employees repo filters `departments.provides_assigned_workers = true` (leftJoin becomes an inner filter, NULL-department workers excluded), and a dedicated `assignedWorkerReferenceOptions()` = `employeesReferenceOptions({activeOnly:true, assignableOnly:true})` (its own cache key — `prefetchClaimEditReferences` warms it too). Admin flags departments via a Da/Ne select on the Odeljenja resource (`settings.departments.manage`, no new permission). `EmployeeSelectField` keeps the claim's current assigned worker selectable even if outside assembly (`currentEmployeeName` prop) so editing never silently drops them. **Trap that bit us (caught by adversarial review):** the EMOTIVE create wizard and DOMACE create form feed ONE `employees` array to BOTH the assigned-worker field AND `StepFaultsFields` — so each create form needs TWO queries (`assignedWorkerReferenceOptions()` for basic, `employeesReferenceOptions({activeOnly:true})` for faults); the detail-edit views already have separate faults sections so they were fine. Regression: `domace-create-worker-source.test.tsx`.
  **(2) Users delete their own notifications.** `DELETE /api/notifications/:id` (one) + `DELETE /api/notifications` (all), gated by the existing `notifications.view_own`, self-scoped (foreign id → 404). One row per recipient, so a delete only clears the caller's own bell. Hard delete (the schema's "no soft delete" comment updated — a notification is a personal log row, not business data). UI: trash per row + "Obriši sve" in the bell, both optimistic. No migration, no new permission.
  Full gate green; **no `db:seed` needed after deploy** — migration `0034` runs via `db:migrate:deploy` and self-flags SKLAPANJE.

- **Employee→department roster load (2026-07-23):** one-off, idempotent bulk load of the real 125-person workforce from the "Lista zaposlenih" Excel — prod previously held only the **15** workers ever fault-attributed on a claim, **none** with a department. `packages/db/src/maintenance/link-employees-to-departments.ts` (`linkEmployeesToDepartments(db, roster)`, exported from `@mr/db`): creates the roster's departments idempotently by `code`, then assigns each worker. **Key trap:** the Excel stores names "Surname Given" but the app stores "Given Surname", and `normalizeName` preserves word order (it is the `normalized_name` unique key) — so matching is by an order-independent `tokenSetKey` (sorted normalized tokens); existing workers are UPDATED (id + fault history preserved), unknown names INSERTed. Runner `apps/api/scripts/link-employees-departments.ts` (dry-run default = run-in-tx-then-throw-to-rollback + report; `-- --apply` commits) loads the committed `apps/api/scripts/employee-roster.json` snapshot (125 workers, 13 new departments — PRIJEM, CISCENJE, CINKOVANJE, ISPITIVANJE, TURBINE, ELEKTRICAR, RADIONICA, SERVIS, NABAVKA, ADMINISTRACIJA, IT, VOZAC, DIREKTOR; existing 8 codes reused, e.g. Excel "KLIPNJAČE"→existing `KLIPNJACE`). Audit/SSE bypassed (bulk bootstrap, like `import-legacy`). **Prod run:** on the api Railway shell, `pnpm --filter api link-employees-departments` (expect 13/114/11/0) then `-- --apply`. No migration, no new permission. To refresh the roster later, regenerate the JSON from the Excel (reorder Surname-Given→Given-Surname, map dept names→canonical codes) and re-run — idempotent.

- **F-A — firm name on the portal header (2026-07-22, `docs/16` §5.1/§5.2):** `GET /api/dashboard/client-summary` now returns `firmNames: string[]` — the firms the CALLING user is linked to (`customer_users`), read from the SAME query that already resolves the own-customer scope (`DashboardRepository.getUserFirms`, renamed from `getUserCustomerIds`; it also feeds SSE customer-channel routing, and deliberately does NOT filter soft-deleted firms because that id set governs claim visibility). The portal header reads it via `usePortalCompany()` instead of each route passing a differently-derived `company` prop — the old `claims.items[0]?.customerName ?? session.user.name` lived in THREE routes, so a client with no claims saw their own personal name. Several firms render "first +N" (`formatCompanyLabel`, §5.3) and a blank name falls back to the account name. An internal full-view actor gets their OWN links (normally `[]`) — never the unscoped set; regression test guards it, since this endpoint's twin leaked once. Also: `requested_company` is cleared inside the approve transaction (the hint is consumed once the approver picks the real firm) with the typed value written into the approve audit entry. **Portal is still EMOTIVE-only** — DOMACE claims have no owner column, so private individuals/domestic firms cannot use the portal at all; deferred by Nikola 2026-07-22.

- **In-app notifications — bell, panel, popups (2026-07-22, `docs/21-notifications-design.md`):** new `notifications` table (migration `0032_motionless_solo`), ONE ROW PER RECIPIENT per event, so read state is per person and the inbox is a single indexed read. Five types: `new_submission` · `claim_created` · `outcome_changed` · `assigned_to_me` · `catalog_added` (create-only, and only for the three catalogs that block claim entry — engine types, manufacturers, customers; its title is neutral "Sistem: …" and never names the admin). The actor is never notified of their own action; `assigned_to_me` wins over `claim_created` for the same user; viewers hold no `notifications.view_own` and get no bell. ⚠️ Recipient resolution must include admins EXPLICITLY — `admin` gets `ALL_PERMISSIONS` from the resolver and may have no `role_permissions` rows, so a naive permission join skips them. SSE `notification_created` is signal-only (id, routed to the recipient's user channel). Popups fire ONLY for notifications arriving while the app is open (never the backlog on load), auto-dismiss in 8 s, ✕ hides without marking read, and snooze (15 min / 1 h / 3 h / tomorrow 08:00) persists server-side in `snoozed_until` while the row stays unread. The DOMACE Excel sheet's NAPOMENA column was removed (2026-07-22) — it carried the single free-text note that multi-row findings replaced, and findings deliberately stay out of the export; the reason (`warrantyReport`) still exports as OPIS PROBLEMA / WARRANTY REPORT. The "Pristiglo" sidebar count badge was REMOVED (notifications replace it) along with its client query factory; `GET /api/client-submissions/pending-count` is left in place with no consumer. **Prod follow-up: run `db:seed` once after deploy for `notifications.view_own`.**

- **SSE transport is now Postgres LISTEN/NOTIFY (2026-07-19, shipped on `worktree-sse-postgres-notify`, commits `7cd670b` onward):** new `PostgresEventBus` (`apps/api/src/modules/events/postgres-event-bus.ts`) COMPOSES the existing `InProcessEventBus` (unchanged — still the fan-out engine, and the test/local double) and adds a transport: publish = fire-and-forget `pg_notify` on one channel (`mr_events`); a dedicated standalone `pg.Client` per replica `LISTEN`s, Zod-validates each inbound message, and replays the matching method onto the wrapped `InProcessEventBus`. Single self-rescheduling guarded reconnect (best-effort, at-most-once — the signal-only SSE contract in §2 is unchanged; payload carries only kind+id, plus customerId for routing, never row data). Wired ONLY in `createContainer` (the prod entrypoint); `buildContainer`'s default param stays `new InProcessEventBus()`, so every test path is unchanged. `server.ts` disposes the bus on shutdown; the `API_REPLICA_COUNT > 1` warning was reworded (not removed) to name the still-in-memory rate limiter (`core/middleware/rate-limit.ts`) as the remaining multi-replica blocker. `numReplicas` stays 1 — this makes SSE replica-safe but the rate limiter (and the permission-cache TTL, docs/05 "Caching strategy") still fragment per process, so multi-replica is deferred. No migration, no new dependency, no new permission, no frontend change.

- **Decouple client view-recording from the detail GET (2026-07-18, commits `d36fdeb`, `7a256ec`, shipped on `refactor/decouple-client-view-recording`):** `GET /api/emotive-claims/:id` is now a pure read — `EmotiveClaimsService.findById` no longer has the Phase-3 side effect of upserting the client's view row. "Seen" is recorded only via the new `EmotiveClaimsService.markClientSeen` / `POST /api/emotive-claims/:id/mark-seen` (204, same `view_own_customer` gate as the GET, Primljeno/non-owned → 404, no permission change, no migration, no audit entry). `apps/portal-web` fires `mark-seen` once on real claim-detail mount (`useEffect`, not the route loader — hovering a list card to prefetch no longer counts as "seen") and on success invalidates the client list + summary queries so the dashboard NEW/UPDATE badge clears without a reload. `clientEmotiveClaimDetailOptions` keeps the normal stale time (`CLAIMS_LIST_STALE_MS`) + `refetchOnWindowFocus: false`; the route component drops the detail query from the cache on unmount instead (`queryClient.removeQueries`, `apps/portal-web/src/routes/claims/$id.tsx`), so re-entering a claim refetches fresh `sectionFreshness` WITHOUT a mount-time background refetch that could race the mark-seen POST — an earlier `staleTime: 0` version had exactly that race (fixed same-day, not shipped to main). The per-visit "Novo" markers stay stable because the GET itself never advances `viewed_at` anymore, and within a visit the cached data stays fresh so mounting never triggers a refetch.

- **EMOTIVE client freshness Phase 3.1 — section markers + chip pulse (2026-07-18, commits `3674660`..`81d3891`, shipped on `client-visibility-phase3.1`):** per-section "Novo" markers on the portal claim detail — see the §2 invariant for the full model. `packages/db` migration `0029_mean_mantis` (nullable `emotive_claims.section_updated_at` jsonb, no backfill — NULL means never bumped); `apps/api` bumps the per-section key at the existing Phase-3 bump sites (inspection/details/outcome/photos) alongside `client_content_updated_at`, and `EmotiveClaimsRepository.findById` computes `sectionFreshness` booleans against the client's pre-open `viewed_at` in the same query as the Phase-3 freshness read; `apps/portal-web` renders per-card "Novo" markers + a `prefers-reduced-motion`-aware pulse on the list chip. EMOTIVE only. **No new permission this phase → no prod seed needed**; migration runs via the existing `db:migrate:deploy` pre-deploy step.

- **EMOTIVE client freshness Phase 3 (2026-07-18, commits `fa8e824`..`c699c6b`, shipped on `client-visibility-phase3`):** NEW/UPDATE badge on the portal claim list — see the §2 invariant for the full model. `packages/db` migration `0028_boring_sphinx` (nullable `emotive_claims.client_content_updated_at` + new `emotive_claim_client_views` table, no backfill needed — NULL means never bumped); `apps/api` bumps the column on client-visible content changes only, adds the unified-list `freshness` LEFT JOIN + CASE (per requesting client user), and upserts a view row on `GET /api/emotive-claims/:id` detail open; `apps/portal-web` renders the chip on `ClientClaimListItem.freshness`. EMOTIVE only — DOMACE untouched. **No new permission this phase → no prod seed needed after deploy**; migration runs via the existing `db:migrate:deploy` pre-deploy step.

- **EMOTIVE client-visibility Phase 2 (2026-07-18, commits `87c4f69`..`85bb8c8`, shipped on `feat/client-visibility-phase2`):** private→published claim lifecycle — see the §2 invariant for the full model. High-level: `packages/db` migration `0027_wonderful_dreaming_celestial` (2 columns + backfill); `packages/shared` (`deriveClientClaimPhase` gains the two-timestamp signature, `clientPhase` added to the client wire, masking in `toClientClaimDetail`/`toClientClaimListItem`, new `emotive_claims.publish` permission + `OPERATOR_PERMISSIONS`); `apps/api` (`EmotiveClaimsService.findById`/`ClaimContextService.loadClaimContext` 404-gate a Primljeno claim for `view_own_customer`, Gate A wired into the inspection-report save path, Gate B = new `POST /:id/publish` endpoint+route+service+repo, outcome email guarded on `published_at`, dashboard client-summary + activity feed made visibility-aware); `apps/internal-web` (publish action + claim stage indicator); `apps/portal-web` (live 3-bar status from server `clientPhase`, Primljeno card non-clickable). DOMACE and internal/statistics/Excel outcome reporting are unchanged — visibility is portal-only. **Prod follow-up (not yet done):** after deploy, run `pnpm --filter @mr/db run db:seed` once to insert `emotive_claims.publish` and grant it to `operator` (seed is additive; admin gets it via the `ALL_PERMISSIONS` bypass already).

- **MR-registry hole incident (2026-07-17, FIXED):** Nikola created a duplicate `7167/25` with no warning and no 409 — prod `mr_registry` held **3 of 127** numbers because `import-legacy` inserts claims directly and never wrote the registry (migration 0010's backfill predates the last legacy import). Fix: (1) approved one-off prod backfill (INSERT…SELECT with `ON CONFLICT (mr_key) DO NOTHING`, 124 rows, verified 0 missing + live warning works); (2) new `backfillMrRegistry(db)` in `packages/db/src/maintenance/` (uses `sqlNormalizeMrKey` — NOTE: pass `sql.raw('col')`, the string overload binds a LITERAL) now runs at the end of every `import-legacy` apply, with integration tests. Deleted claims correctly stay unregistered (release-on-delete is design).

- **Grupa D — business-value features (2026-07-17, commits `59b3719`, `22455d5`, `af1c97d`, full gate green, NOT yet pushed):** (D1) MR-number duplicate pre-flight — `@mr/shared` mr-registry query factory + debounced warning under the MR field in BOTH create forms with a link to the owning claim; `ApiError` now carries envelope `details` (was dropped in `parseApiErrorBody`) so the MR-key 409 also links to the conflicting claim. Warning is `checkMrDuplicate` opt-in because detail-edit reuses the same field components (own MR would false-positive). (D2–D4) Statistics: three new `/summary` sections honoring all existing filters — `domaceAmounts` (SUM of DOMACE total_amount + count, new KPI cell, EUR), `byCustomer` (EMOTIVE per-partner with outcome counts, "Po partneru" rank chart), `byFaults` (first read of fault tables: per employee/department/external party, "Kvarovi" 3-card section). Integration tests isolate via a per-test manufacturer filter (container writes COMMIT through the pool — rows persist across tests AND runs; year buckets alone are not safe). (D5) EMOTIVE outcome-change → signal-only bilingual email to approved portal users of the claim's customer (fire-and-settle, EmailPort-gated, admin toggle `app_settings emotive_claims.notify_client_on_outcome='false'`, new repo lookup `getOutcomeNotificationRecipients`; DOMACE excluded — no customer FK). Remaining D candidates NOT built: fault-analytics deep-dive beyond ranks, `employee_monthly_output` writer (Excel PROCENAT — needs its own design), byCustomer for DOMACE (needs firms design, docs/16).

- **Pre-commit deploy audit (2026-07-06/07, multi-agent, all fixes applied):** (1) BLOCKER — internal-web prod bundle 500'd on every route: Nitro's externals tracer ships a partial `tslib` when direct `@radix-ui` deps (vendored TipTap primitives) are externalized; fixed via `/^@radix-ui\//` in `ssr.noExternal` (internal-web only — other apps have no direct radix deps), proven by booting the built server (`/login` 200). (2) One-off scripts now run in the prod image: `COPY scripts` in Dockerfile (create-admin), `--env-file=.env` dropped from `import-legacy`/`recompress-attachments` (in-script `process.loadEnvFile` try/catch — dev unchanged, Railway env works), `import-legacy -- --file <path>` override (runbook: upload JSON to `/data`). (3) Dockerfile: `pnpm fetch` dep layer keyed on lockfile. (4) docs/11 + `.env.example` accuracy pass (public paths list, optional-var semantics, HOST=`::` — Railway private net is IPv6, API_BASE_URL/BETTER_AUTH_URL = public interno origin since signed attachment URLs are browser-facing, UptimeRobot via proxy URL, VITE_API_URL prod warning); `packages/email/node_modules` added to compose shield list. (5) SECURITY FIX (approved 2026-07-07): client IP now derived ONLY via `clientIpOf` (`apps/api/src/core/http/client-ip.ts`) — `CF-Connecting-IP` first (CF overwrites it; trust requires the default `*.up.railway.app` web domains be DISABLED, runbook/checklist item), else RIGHTMOST `X-Forwarded-For` entry (proxy-appended), validated with `net.isIP`, else null. Both consumers (rate-limit keying, audit `actorIp`) route through it; `x-real-ip` no longer read; regression tests incl. forged-leftmost-XFF bypass. **Follow-up (2026-07-15):** Better-Auth's OWN IP path (the Login audit row / `session.ipAddress`) previously bypassed `clientIpOf` and used the library-default forgeable LEFTMOST `x-forwarded-for`; now pinned to `cf-connecting-ip` via `advanced.ipAddress.ipAddressHeaders: ['cf-connecting-ip']` in `packages/auth/src/options.ts` (NOT `+ x-forwarded-for` — Better-Auth reads it leftmost too, which would reintroduce the forgeability), with an integration regression test (`packages/auth/src/__tests__/integration/login-ip-source.integration.test.ts`) and `docs/01` reconciled to the implemented value. Deferred: container runs as root (Railway isolation + Chromium path complexity).

- **Deploy prep (2026-07-06, D-1..D-4 DONE, deploy itself pending):** D-1 seeds split (`1d532af`), D-2 `/api` proxy (`64eb668`), D-3 = `db:migrate:deploy` (extensions + migrations, works from-zero — extensions can NOT be a migration file, `citext` is needed by `0000`), production `apps/api/Dockerfile` (compose `prod-like` overrides CMD to dev), `railway.json` in all 4 app dirs (api: Dockerfile builder + `preDeployCommand` migrator + `/health`; web: Nixpacks turbo build + `/login`), D-4 = `docs/11` rewritten to reality (in-app proxy routing, real env list from `apps/api/src/config/env.ts`, first-deploy runbook, api has NO public domain). Next: actual Railway setup with Nikola (account, env vars, volume, DNS).

- **NEXT UP — Machining claims + Firms design (2026-07-06):** `docs/16-machining-and-firms-design.md` is the approved-direction design doc (status: PREDLOG reviewed with Nikola, implementation NOT started). Key decisions: machining = ONE new claim family `machining_claims` (NOT a third EMOTIVE/DOMACE variant — orthogonal axes), `machining_part_types` catalog (7th catalog-pattern instance, seed glava/blok/radilica), part↔engine claim link via one-of `linked_emotive_claim_id`/`linked_domace_claim_id`, radni nalog = the only required form field. Firms: portal header company from `client-summary` firmNames (customer_users link, not first claim), inline "+ Nova firma" in the approve dialog (same customers.create endpoint), multi-firm prepared not built (norm 1 account = 1 firm). Build order: F-A (header) → F-B (inline firm) → M-0..M-3 (machining; M-0/M-1 are migrations → explicit approval). Start each phase only on Nikola's go.

- **Optimization pass (July 2026, after the four-agent health audit)** — implemented across the stack:
  - **Images**: every uploaded photo is recompressed at upload (`optimizeAttachmentImage`, max 2048px/q80 — only optimized bytes are stored); download endpoint gained `?variant=thumbnail` (grids use it via `buildAttachmentThumbnailUrl`), ETag from `content_sha256` + `Cache-Control: private, max-age=86400` for inline images (304 revalidation; documents stay no-store), and file bodies now STREAM (`storage.readStream`). One-off `pnpm --filter api recompress-attachments` (dry-run; `-- --apply`) recompresses pre-existing photos.
  - **API**: global `requestBodyLimit` (2 MB default / 130 MB upload paths — throws 413 before buffering); `ClaimReportPdfRenderer` in the container (ONE shared Chromium, new context per render, 2-slot cap, disposed on shutdown + in tests); claims list count+page run in `Promise.all`; event bus `setMaxListeners(0)`; `/health` excluded from request logs.
  - **DB (migration `0022_elite_franklin_richards`, proven migrate-from-zero)**: `idx_audit_log_created_at_id`, `idx_audit_log_entity_type_created_at`, fault-table `external_party_id` indexes, and ALIGNED full-text search — `idx_emotive_claims_search_fts` (warranty+mr_number), `idx_domace_claims_search_fts` (warranty+mr_number+customer_name), `idx_customers_name_fts`; repository expressions must stay TEXTUALLY identical to these (unified search matches customers via an indexed semi-join). Statistics: emotive anchor uses raw `date_of_claim` (NOT NULL), resolved-range compares the raw timestamptz column (no `::date` cast) — index-friendly, semantics unchanged.
  - **Frontends**: pathless `_shell` layout routes in internal/admin (shell + SSE stream survive navigations; admin role guard hoisted); `defaultPreloadDelay: 100` everywhere; claim-detail reference prefetch is fire-and-forget; recharts split out of the internal entry (269→143 KB gz); logo = one theme-appropriate 512px img (~37 KB, was ~1 MB both variants); claim-report editor serializes on a 300 ms quiet window with blur/unmount flush + memoized EditorContext; capture-flag listener leak fixed.
  - **Hygiene**: turbo outputs corrected (no more warnings; `test:integration` uncacheable; `lint` depends on `^build`; `@mr/tailwind-preset` has a build stub so token changes bust dependent caches — the old stale-CSS pain), `*.zip` gitignored, zod aligned to 4.4.2 everywhere, dead code removed (incl. 33 dead i18n keys), `formatFieldError` + `createAppQueryClient` deduped into `@mr/shared`.
  - **Deliberately NOT done**: `locale.ts`/`theme.ts`/`user-menu` per-app copies (stable duplication, hoist later), keyset pagination for claims, domace anchor expression index, Serbian FTS stemmer, Brandbook PDF git-history rewrite. (Note: permission resolution IS cached — `cachedByRoles`, 5-min TTL keyed by sorted role codes; the residual per-request cost is Better-Auth session validation + one indexed roles lookup, an accepted tradeoff, not an open item.)

- **Portal v2 redesign ("Precision Engineering", July 2026)** — implemented from `design_handoff_client_portal` (zip in repo root; README = spec, `MR Portal v2.dc.html` = authoritative desktop reference). Key facts:
  - Screens: login/register (split hero + marquee), `/pending` (after signup AND on pending login), welcome (hero-misa), dashboard (`/claims` = stats + filter + 2-col cards + activity/support rail), detail (`/claims/$id` = timeline, basics, inspection, photos+lightbox, PDF popup viewer, technician card).
  - Tokens: portal-scoped `--mrp-*` vars in `apps/portal-web/src/styles/globals.css` (dark default + `.light` on `<html>`); theme persisted in localStorage (`mrr:portal:theme`) with a pre-paint bootstrap script; **portal default locale = EN** (`syncPortalRequestLocale` + `PORTAL_LOCALE_BOOTSTRAP_SCRIPT` in `@mr/i18n` — cookie wins, Accept-Language deliberately ignored).
  - Server-derived 3-phase status: as of 2026-07-05 (superseded 2026-07-18, see the client-visibility Phase 2 invariant in §2) this was a pure function of `outcome` — `deriveClientClaimPhase(outcome)`: pending → `in_progress`, resolved → `outcome`. The redundant `progressPhase` wire field was REMOVED then — the portal read status from `outcome` via the same shared function (no client-side re-derivation). Phase 2 re-introduced a wire field (`clientPhase`, now a function of `outcome` **plus** `clientVisibleAt`/`publishedAt`, not outcome alone) because the portal must no longer see the real outcome before publish; `ClientClaimPhase.Received` (Primljeno) is now a live, non-clickable stage, not just a static timeline step. Detail also exposes `employeeName` (approved 2026-07-03). `GET /api/dashboard/client-summary` (phase counts + audit-projected activity feed, own-customer scoped, visibility-aware since Phase 2). Client PDF: `GET /api/claim-reports/export/client/pdf` (`export.own_claims`, 404 for non-owned, also 404 while Primljeno) + in-app iframe viewer.
  - Service filter (All | Engine remanufacture | Machining) is prepared UI: everything renders as engine remanufacture until machining claims exist internally (`claimServiceType()` in portal is the single point to wire the real field later).
  - Support emails: `SUPPORT_EMAIL_BY_KIND` (EMOTIVE → claims@mrgroup.rs, DOMACE → reklamacije@mrgroup.rs), phone +381 11 344 5566 static (`@mr/shared/constants/support-contact.ts`).
  - Deliberately NOT built: forgot-password link (no reset flow exists — needs its own approved task), red primary buttons (forbidden by brandbook), client claim creation.

- `fix(db)`: backfill integration test now seeds permissions before roles → `test:integration` green & order-independent.
- `perf(db)`: migration **0018** added indexes on `emotive_claim_faults` & `domace_claim_faults` `claim_id` / `employee_id` / `department_id` (proved clean migrate-from-zero; EXPLAIN shows index scan).
- `refactor(admin)`: user role/status badges routed through `mr-*` tokens + `BADGE_SHELL_CLASSES`; added **`mr-accent`** (teal) as the 6th hue for the client role; domace success box → `mr-success`; documented in `docs/09` + `docs/15`.

**Open (from the health audit — not yet approved/done):**

- nice-to-have: `~117 @ts-nocheck` in vendored TipTap components (confirmed **leave as-is**). The few remaining `any`/`eslint-disable` in internal-web are vendored TipTap support hooks (`use-throttled-callback.ts`, `use-unmount.ts` — already `@ts-nocheck`'d), **not our code** — `api`/`shared`/`admin-web` are `any`-free.
- attachment signing secret: code now reads `env.ATTACHMENT_SIGNING_SECRET` with fallback to `BETTER_AUTH_SECRET` (so behaviour is unchanged until set). **To activate defense-in-depth, Nikola must add `ATTACHMENT_SIGNING_SECRET` (≥32 chars) to the API env** — introducing/rotating it invalidates outstanding signed URLs (5-min TTL, negligible). (`turbo.json` web-output cache miss resolved in commit `e611738`.)
- Roadmap: project is past Phase 0/1 foundations; admin user/role management is in active build (Phase 3 territory). Custom outcomes are explicitly a **Phase 2 big project** (6-place hardcoding) — don't start casually.

---

_Keep this file current: when a rule changes or drift is resolved, update the relevant section here in the same change._
