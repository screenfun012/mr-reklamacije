# API origin used only during SSR (Nitro/Node) because Node cannot
# resolve relative URLs like '/api/auth' through the server file
# route proxy. In the browser, authClient uses window.location.origin
# and goes through the proxy, so this variable is ignored there.
VITE_API_URL=http://localhost:3000
