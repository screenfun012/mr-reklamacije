import { schema } from '@mr/db'

export const intakeOrders = schema.intakeOrders
export const attachments = schema.attachments
export const users = schema.users
export const auditLog = schema.auditLog
